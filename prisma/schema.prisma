generator client {
  provider = "prisma-client"
  output   = "../src/generated/prisma/generate-client-db1"
}

datasource db {
  provider = "postgresql"
}

model User {
  id    Int     @id @default(autoincrement())
  email String  @unique(map: "email") @db.VarChar(255)
  name  String?
}
